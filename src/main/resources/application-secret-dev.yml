spring:
  config:
    activate:
      on-profile: secret-dev
  datasource:
    url:
    username:
    password:
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host:
      port: 6379
auth:
  jwt:
    secret:
    access-expire:
    refresh-expire:
  x-api-key:
