spring:
  config:
    activate:
      on-profile: local
  jpa:
    show-sql: true
    generate-ddl: true
    properties:
      "[hibernate.hbm2ddl.auto]": update
      "[hibernate.format_sql]": true
logging:
  level:
    "[org.hibernate]": debug

